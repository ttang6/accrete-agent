# Mutation generated 2026-05-03T19:38:42
# Backend active lessons (mutation targets):
#   - b06551e9 schema_mismatch on skill_exec:ai-digest/dup_check
#   - ae7e3706 tool_runtime_error on skill_exec:ai-digest/dup_check
#   - b627da82 coverage_gap on (none)
# Token usage: input=1575, output=1291 (gpt-4o)
# 人工 review 后拆到 evals/tasks/<id>.yaml

```yaml
- id: mut2_reminder_no_context
  description: 高级助理：提醒我未来会议但没上下文（容易触发 schema_mismatch）
  query: "帮我标记一下，这个月内的几次重要会议，不要重复提到"
  expected_obligations: []
  expected_tool_calls:
    - {tool: skill_exec, skill: ai-digest, script: dup_check}
  expected_coverage: []
  max_iterations: 30
  success_keywords: ["会议", "重要"]
  expected_failure_type: # schema_mismatch, lesson_id=b06551e9

- id: mut2_specify_projects_but_vague
  description: 开发者需求：标记今天提到的项目但描述模糊（可能触发 tool_runtime_error）
  query: "今天提到的几个有趣的项目，麻烦帮我记下来，以后不再重复推荐"
  expected_obligations:
    - mark_digest_when_user_asks_to_record
  expected_tool_calls:
    - {tool: skill_exec, skill: ai-digest, script: fetch_github}
    - {tool: skill_exec, skill: ai-digest, script: dup_check}
  expected_coverage: [oss]
  max_iterations: 35
  success_keywords: ["项目", "记录"]
  expected_failure_type: # tool_runtime_error, lesson_id=ae7e3706

- id: mut2_last_hour_update
  description: 获取过去 1 小时的更新，新闻还没出现新内容（导致 coverage_gap）
  query: "帮我看看过去 1 小时有啥新闻更新，若没有也告诉我"
  expected_obligations: []
  expected_tool_calls:
    - {tool: skill_exec, skill: ai-digest, script: fetch_rss}
  expected_coverage: [news]
  max_iterations: 30
  success_keywords: ["新闻", "更新"]
  expected_failure_type: # coverage_gap, lesson_id=b627da82

- id: mut2_save_list_without_action
  description: 请助理存储一列项目但未提供具体动作（可能触发 schema_mismatch）
  query: "这些 Python 项目不错，帮我保存一下下次查看"
  expected_obligations: []
  expected_tool_calls:
    - {tool: skill_exec, skill: ai-digest, script: dup_check}
  expected_coverage: []
  max_iterations: 30
  success_keywords: ["项目", "保存"]
  expected_failure_type: # schema_mismatch, lesson_id=b06551e9

- id: mut2_ambiguous_mark_request
  description: 专家请求记录成果但过于模糊（容易触发 tool_runtime_error）
  query: "今天我们讨论的几个重要成果给我标记一下，避免下次重复"
  expected_obligations:
    - mark_digest_when_user_asks_to_record
  expected_tool_calls:
    - {tool: skill_exec, skill: ai-digest, script: fetch_hf}
    - {tool: skill_exec, skill: ai-digest, script: dup_check}
  expected_coverage: [paper]
  max_iterations: 35
  success_keywords: ["成果", "记录"]
  expected_failure_type: # tool_runtime_error, lesson_id=ae7e3706

- id: mut2_skip_news_for_paper
  description: 学者只要最新论文排除新闻（触发 coverage_gap）
  query: "只把今天最值得看的论文发我，新闻部分省略"
  expected_obligations: []
  expected_tool_calls:
    - {tool: skill_exec, skill=ai-digest, script=fetch_hf}
  expected_coverage: [paper]
  max_iterations: 30
  success_keywords: ["论文", "最新"]
  expected_failure_type: # coverage_gap, lesson_id=b627da82

- id: mut2_unclear_task_save
  description: 助手请求保存内容但未详细说明（容易触发 schema_mismatch）
  query: "今天的一些重要讨论内容保存一下，之后用"
  expected_obligations: []
  expected_tool_calls:
    - {tool: skill_exec, skill: ai-digest, script: dup_check}
  expected_coverage: []
  max_iterations: 30
  success_keywords: ["讨论", "保存"]
  expected_failure_type: # schema_mismatch, lesson_id=b06551e9

- id: mut2_collect_repos_without_details
  description: 产品经理请求收集项目但缺少具体细节（可能触发 tool_runtime_error）
  query: "今天发布会展示的项目集，帮我标记着，不需重复看到"
  expected_obligations:
    - mark_digest_when_user_asks_to_record
  expected_tool_calls:
    - {tool: skill_exec, skill: ai-digest, script: fetch_github}
    - {tool: skill_exec, skill: ai-digest, script: dup_check}
  expected_coverage: [oss]
  max_iterations: 35
  success_keywords: ["项目", "标记"]
  expected_failure_type: # tool_runtime_error, lesson_id=ae7e3706

- id: mut2_hourly_update_without_news
  description: 检查最近 3 小时动态时未能获取新闻（产生 coverage_gap）
  query: "给我更新最近 3 小时内的一切重要动态，尤其是技术类"
  expected_obligations: []
  expected_tool_calls:
    - {tool: skill_exec, skill=ai-digest, script=fetch_github}
    - {tool: skill_exec, skill=ai-digest, script=fetch_hf}
    - {tool: skill_exec, skill=ai-digest, script=fetch_rss}
  expected_coverage: [oss, paper]
  max_iterations: 40
  success_keywords: ["动态", "技术"]
  expected_failure_type: # coverage_gap, lesson_id=b627da82
```